# Verification evidence

OpenAllay adopted its current name on 2026-07-19. Evidence captured before
that cutover is intentionally immutable: screenshots, reports, logs, and
checksums may therefore display the former identity. Those artifacts describe
historical test runs and are not current product metadata or compatibility
aliases.

Post-cutover evidence is stored under `openallay-cutover/`.
